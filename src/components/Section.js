import React from "react"
import sectionStyles from "./Section.module.css"

const Section = props => (
  <section>
    <div className="container sectionStyles.section-container"></div>
  </section>
)

export default Section
